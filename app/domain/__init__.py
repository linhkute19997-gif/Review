"""
Domain layer
============
Pure-Python core for Review Phim Pro: data models, the pipeline runner,
project-file persistence and the render queue. No PyQt6, no FFmpeg,
no I/O outside the standard library lives in here so the layer stays
unit-testable and reusable from a future CLI.
"""
